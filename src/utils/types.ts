export type TFact = {
  fact: string;
  lenght: number;
};

export type FactsFormValues = {
  fact?: string;
};

export type NameFormValues = {
  name: string;
};

export type TAge = {
  count: number;
  name: string;
  age: number;
};
